using Godot;
using System;
//using DialogueManagerRuntime;

public partial class TempTestDialogue : Node3D
{
	/*
	[Export(PropertyHint.File)]
	string dialogueResourcePath;
	
	Resource dialogueResource;
	// Called when the node enters the scene tree for the first time.
	public async override void _Ready()
	{
		dialogueResource = GD.Load<Resource>(dialogueResourcePath);
		DialogueManager.ShowExampleDialogueBalloon(dialogueResource, "this_is_a_node_title");

		//var line = await DialogueManager.GetNextDialogueLine(dialogueResource);
		//GD.Print(line.ToString());
	}

	// Called every frame. 'delta' is the elapsed time since the previous frame.
	public override void _Process(double delta)
	{

	}
	*/
}
